import { css } from '@emotion/react';

export const scrollButtonStyle = css`
  position: absolute;
  bottom: 13%;
  left: 50%;
  transform: translateX(-50%);
  z-index: 10;
  background-color: inherit;
  border: none;
  padding: 0;
  cursor: pointer;
`;
